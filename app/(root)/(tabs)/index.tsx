import { SafeAreaView } from "react-native-safe-area-context";
import { StatusBar } from "expo-status-bar";
import { Text } from "react-native";

const chat = () => {
  return (
    <SafeAreaView>
      <Text>Sucessfully Implemented Authentication</Text>
      <StatusBar style="dark" />
    </SafeAreaView>
  );
};

export default chat;
